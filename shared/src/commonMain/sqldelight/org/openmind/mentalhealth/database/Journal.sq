CREATE TABLE journal (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    text TEXT NOT NULL,
    mood TEXT NOT NULL,
    timestamp INTEGER NOT NULL
);

insertJournal:
INSERT INTO journal(text, mood, timestamp)
VALUES (?, ?, ?);

selectAll:
SELECT * FROM journal;
